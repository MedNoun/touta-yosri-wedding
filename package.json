{
  "name": "touta-yosri-wedding",
  "version": "1.0.0",
  "description": "Wedding invitation website for Eya & Yosri",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build": "npm run build:analytics",
    "build:analytics": "esbuild js/analytics.js --bundle --outfile=js/analytics.bundle.js --format=iife --minify",
    "test": "echo 'No tests configured'"
  },
  "keywords": [
    "wedding",
    "invitation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
